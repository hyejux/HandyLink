<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.HiMade.user.mapper.UserChatRoomMapper">
    <insert id="insertChat" parameterType="com.example.HiMade.user.dto.UserChatDTO">
        INSERT INTO CHAT (chat_content, chat_time, store_id, user_id, sender_type)
        VALUES (#{chatContent}, CURRENT_TIMESTAMP, #{storeId}, #{userId}, #{senderType})
    </insert>

    <select id="getChat" resultType="com.example.HiMade.user.dto.UserChatDTO">
        SELECT * FROM CHAT
        WHERE (#{storeId} = store_id AND #{userId} = user_id)
        ORDER BY send_time
    </select>
</mapper>