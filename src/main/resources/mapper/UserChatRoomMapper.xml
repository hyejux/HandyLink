<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.HiMade.user.mapper.UserChatMapper">
    <insert id="insertChatRoom">
        INSERT INTO chat_room (store_id, user_id)
        VALUES (#{storeId}, #{userId})
    </insert>

    <select id="selectChatRoomByUser" resultType="com.example.HiMade.user.dto.UserChatDTO">
        SELECT * FROM chat_room
        WHERE user_id = #{userId}
    </select>
</mapper>